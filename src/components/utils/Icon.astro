---
//TS Support to define correctly variables
export interface Props {
  icon: string;
  folder: string;
  variant: string;
}
const { icon, folder, variant } = Astro.props;
const { default: innerHTML } = await import(
  `../../icons/${folder}/${icon}${variant}.svg?raw`
);
---

<Fragment set:html={innerHTML} />
